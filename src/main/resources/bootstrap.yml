spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
        name: gateway-service
        refresh-enabled: true
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
